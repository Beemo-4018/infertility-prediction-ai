# 난임 환자 임신 성공 여부 예측 해커톤 PPT 구성안

## 1. 발표 목표

이 발표의 목적은 단순히 최고 점수만 보여주는 것이 아니라, 아래 4가지를 설득력 있게 전달하는 데 있다.

- 데이터의 구조와 어려움을 어떻게 해석했는지
- 어떤 실험 전략이 실제로 성능 개선으로 이어졌는지
- 무엇이 실패했고, 왜 실패했다고 판단했는지
- 현재 기준 모델 `v7b`가 왜 가장 합리적인 기준선인지

현재 기준 요약:

- 정식 기준 모델: `train_v8plus_v7b.py`
- 빠른 실험 기준: `train_v8plus_v7b_lgb_screen.py`
- 로컬 최고 CV: `0.74083`
- 최고 제출 점수: `0.74216`


## 2. 20분 발표용 전체 흐름

20분이면 대략 14~16장 정도가 가장 안정적이다.  
추천은 본문 15장 + 부록 2~3장이다.

시간 배분 추천:

1. 문제와 데이터 이해: 3분
2. 베이스라인과 검증 체계: 3분
3. 실험 전략 변화와 버전 진화: 7분
4. 실패 실험과 인사이트: 3분
5. 최종 모델과 향후 계획: 4분

핵심은 슬라이드 수를 늘리는 것보다, 각 장이 하나의 메시지만 가지게 만드는 것이다.


## 3. 추천 목차

1. 대회 개요와 문제 정의
2. 데이터 특성과 주요 난점
3. 평가 지표와 검증 전략
4. 초기 베이스라인 구조
5. 전처리와 feature engineering 기본 원칙
6. 실험 전략 전환: 피처 다이어트에서 단일 변경 검증으로
7. 버전별 성능 변화
8. 성능을 실제로 올린 핵심 피처들
9. 시술 시기 코드가 중요한 이유
10. 실패한 실험과 배운 점
11. 현재 최종 기준 모델 `v7b`
12. 최근 스크리닝 실험 결과
13. 최종 결론
14. 향후 계획

부록:

- feature importance 상세
- 결측/이상치 스크리닝 표
- 제출 점수 이력


## 4. 슬라이드별 상세 구성

### 슬라이드 1. 제목

제목 예시:

- 난임 환자 임신 성공 여부 예측 모델 개발
- `v7b` 기준 실험 전략과 성능 개선 과정

넣을 내용:

- 팀명
- 대회명
- 발표자
- 평가지표: ROC-AUC

발표 멘트 포인트:

- 이번 발표에서는 최고 점수 자체보다, 그 점수에 도달한 실험 흐름과 판단 기준을 설명한다.


### 슬라이드 2. 문제 정의

넣을 내용:

- 입력: 환자 정보, 시술 이력, 배아/난자/정자 관련 정보
- 출력: 임신 성공 여부 확률
- 목표: ROC-AUC 최대화

강조 포인트:

- 의료 데이터라서 단순 분류 문제가 아니라, 결측과 범주의 의미를 해석하는 것이 중요했다.


### 슬라이드 3. 데이터 개요

넣을 숫자:

- train: `256,351 x 69`
- test: `90,067 x 68`
- 임신 성공률: `25.83%`
- 클래스 불균형 비율: 약 `2.87 : 1`

추천 시각화:

- 타깃 비율 bar chart
- 변수 유형 비율 요약

말할 포인트:

- 샘플 수는 충분하지만, 결측과 희소 범주가 많아 단순 모델링만으로는 한계가 있었다.


### 슬라이드 4. 데이터의 핵심 난점

넣을 내용:

- 고결측 컬럼 존재
- 의미가 강한 범주형 변수 다수
- 시술 맥락에 따라 같은 값의 의미가 달라짐
- 일부 파생은 좋아 보여도 실제 CV에서는 쉽게 무너짐

예시 컬럼:

- `배아 해동 경과일`
- `난자 해동 경과일`
- `시술 시기 코드`
- `특정 시술 유형`

메시지:

- 이 문제는 “좋아 보이는 아이디어 많이 넣기”보다, “한 개씩 검증해서 살아남은 것만 남기기”가 중요했다.


### 슬라이드 5. 평가 지표와 검증 전략

넣을 내용:

- 평가지표: ROC-AUC
- 검증 방식: Stratified 5-Fold OOF
- 빠른 스크리닝: LightGBM 5-fold
- 최종 비교: LGB + XGB + CAT 앙상블

강조 포인트:

- 모든 실험은 같은 기준에서 비교해야 의미가 있다.
- 그래서 `v7b_screen`을 빠른 실험 기준선으로 사용했다.


### 슬라이드 6. 초기 베이스라인 구조

넣을 내용:

- 모델: LightGBM, XGBoost, CatBoost
- 기본 전처리:
  - count 파싱
  - 결측 처리
  - 비율 피처
  - OOF target encoding
- 최종 예측: 앙상블

추천 시각화:

- 데이터 흐름도
  - Raw data
  - preprocess
  - feature engineering
  - 3 models
  - ensemble


### 슬라이드 7. 전처리 원칙

현재 `v7b` 기준으로 정리:

- 수치형 결측: train 기준 median
- 범주형 결측: `Unknown`
- 일부 시간 컬럼: 결측 플래그 생성
- target encoding은 반드시 OOF 방식
- leakage 방지 우선

발표 포인트:

- 복잡한 imputation보다, 안정적이고 재현 가능한 처리 방식을 우선했다.


### 슬라이드 8. 실험 전략 전환

핵심 메시지:

- 초반에는 피처 다이어트 중심으로 접근
- 이후 결과를 보니, 추가 다이어트보다 도메인 파생이 더 효과적
- 그래서 `v5` 이후부터는 단일 파생 추가 중심으로 전략 전환

꼭 넣을 문장:

- “좋아 보이는 대규모 변경보다, 검증 가능한 단일 변경이 더 잘 먹혔다.”


### 슬라이드 9. 버전별 성능 변화

표 예시:

| 버전 | 핵심 변화 | CV |
|------|----------|----|
| v3 | 피처 다이어트 | 0.74076 |
| v4 | 과도한 제거 | 0.74065 |
| v5 | 파생변수 추가 | 개선 전환점 |
| v6e | `시술시기코드_난자정자조합_te` | 0.74079 |
| v7b | `고령_x_단일배아이식` | 0.74083 |

추천 시각화:

- 버전별 CV line chart

포인트:

- 성능 향상은 큰 구조 변경보다, 해석 가능한 작은 변경에서 나왔다.


### 슬라이드 10. 실제로 효과 있었던 피처

넣을 후보:

- `난자정자출처조합_te`
- `시술시기코드_난자정자조합_te`
- `고령_x_단일배아이식`
- `총배아대비_해동비율`
- `IVF_실패부담`

추천 자료:

- feature importance top 15
- 각 피처의 해석 한 줄씩

발표 포인트:

- 중요한 피처들은 대부분 “도메인 해석이 가능한 조합형 피처”였다.


### 슬라이드 11. 시술 시기 코드가 중요한 이유

넣을 내용:

- train/test 모두 동일한 코드 집합
- unseen code 없음
- 코드별 성공률 차이 존재
- 다른 변수와 결합할 때 신호가 강해짐

추천 시각화:

- 코드별 성공률 막대그래프
- train/test 코드 분포 비교

강조 포인트:

- `시술 시기 코드`는 단순 ID가 아니라 환경과 프로토콜 차이를 반영하는 proxy 변수로 해석할 수 있다.


### 슬라이드 12. 실패한 실험과 배운 점

넣을 사례:

- 추가 피처 다이어트 실패
- `배란장애_x_배란유도유형_te` 최종 결합 실패
- `이식일5_단일이식` 실패
- `저장배아_최적` 실패
- count clipping 기반 이상치 처리 대부분 실패
- 결측 처리 `v7i/j/k`도 유의미한 개선 없음

핵심 메시지:

- 직관이 좋아 보여도, 실제로는 기존 강한 피처와 중복되어 성능이 떨어질 수 있다.

추천 시각화:

- “아이디어 / 기대 / 실제 결과 / 판단” 4열 표


### 슬라이드 13. 현재 기준 모델 `v7b`

넣을 내용:

- 정식 기준 코드: `train_v8plus_v7b.py`
- 로컬 최고 CV: `0.74083`
- 최고 제출 점수: `0.74216`
- 이후 모든 실험의 기준선 역할

발표 포인트:

- 지금은 `v7b`를 흔들지 않고, 그 위에 단일 변경 실험만 얹는 방식으로 운영하고 있다.


### 슬라이드 14. 최근 스크리닝 실험 결과

현재 확인된 결과:

| 실험 | 의미 | screen CV | 기준 대비 |
|------|------|-----------|----------|
| v7b_screen | 기준 | 0.73918 | 기준 |
| v7f | 저장된 배아 수 clip | 0.73921 | +0.00003 |
| v7i | 시간 결측 플래그 추가 | 0.73908 | -0.00010 |
| v7j | 시간 컬럼 sentinel 처리 | 0.73913 | -0.00005 |
| v7k | 배아정보 통결측 플래그 | 0.73909 | -0.00009 |

메시지:

- 이상치/결측 처리 쪽은 아직 확실한 승자가 없고, `v7f`만 아주 약한 후보였다.
- 따라서 무리하게 반영하지 않고 정식 검증을 먼저 진행했다.


### 슬라이드 15. 최종 결론

한 장으로 정리할 문장:

- 가장 효과적이었던 것은 대규모 구조 변경이 아니라, 데이터 의미를 반영한 단일 파생 검증이었다.
- `v7b`는 현재 가장 안정적인 기준 모델이다.
- 실패 실험도 방향성을 정리하는 데 중요한 역할을 했다.

마무리 문장 예시:

- “이번 실험에서 얻은 핵심은 복잡한 기법보다, 검증 가능한 단일 변경과 일관된 기준선 운영이 성능 개선에 더 중요하다는 점입니다.”


### 슬라이드 16. 향후 계획

넣을 내용:

- `v7b` 기준 단일 변경 실험 지속
- 희소 범주 병합, 조건부 결측 처리, 시술 시기 코드 기반 추가 조합 검토
- 스크리닝 통과 후보만 정식 앙상블로 승격

마무리 포인트:

- 현재는 탐색을 계속하되, 검증 기준은 더 엄격하게 가져간다.


## 5. 20분 발표에서 꼭 추가하면 좋은 내용

발표 시간이 20분이면, 아래 4가지는 꼭 넣는 것이 좋다.

### 1. 실패 실험도 짧게 보여주기

단순히 최고 점수만 발표하면 실험이 얕아 보일 수 있다.  
실패 사례를 1장 정도 넣으면 팀의 판단 과정이 더 설득력 있어진다.

추천 실패 사례:

- 피처 다이어트 과도 적용
- 직관적 파생 추가 실패
- 결측/이상치 처리 미세 조정 실패


### 2. “왜 이 피처가 먹혔는가”를 도메인 언어로 설명하기

예:

- 고령과 단일 배아 이식의 조합은 임상적으로도 시사점이 있다
- 시술 시기 코드는 단순 코드가 아니라 시술 환경의 대리 변수일 수 있다
- 난자/정자 출처 조합은 성공률 차이를 설명하는 중요한 축이다


### 3. 제출 점수와 로컬 CV 차이를 한 번 짚기

넣을 내용:

- 로컬 최고 CV: `0.74083`
- 최고 제출: `0.74216`

설명 포인트:

- 로컬 검증이 완벽히 LB를 대체하지는 않지만, 방향성 판단에는 충분히 유효했다.


### 4. 실험 운영 체계 자체를 장점으로 보여주기

발표에서 강조할 수 있는 운영 포인트:

- 기준선 고정: `v7b`
- 빠른 screening과 정식 검증 분리
- 단일 변경 원칙 유지
- 실패 실험도 기록 및 재활용


## 6. 발표자용 한 줄 메시지 모음

슬라이드마다 발표자가 짧게 강조할 수 있는 문장들:

- 이 문제의 핵심은 모델보다 데이터 해석이었습니다.
- 저희는 큰 구조 변경보다 단일 변경 검증을 택했습니다.
- 실제 성능 향상은 해석 가능한 조합형 피처에서 나왔습니다.
- 좋아 보이는 아이디어도 기준선 위에서 검증하면 대부분 탈락했습니다.
- 그래서 현재는 `v7b`를 고정 기준으로 유지하고 있습니다.


## 7. 예상 질문과 답변 포인트

### Q1. 왜 CatBoost 단독보다 앙상블을 선택했나요?

- 각 모델이 포착하는 패턴이 조금씩 다르기 때문이다.
- 다만 최근에는 모델 다양성보다 피처 개선의 기여가 더 컸다.


### Q2. 왜 복잡한 imputation을 쓰지 않았나요?

- 결측 자체가 중요한 신호일 수 있어서 단순 대체가 항상 유리하지 않았다.
- 재현성과 leakage 방지 측면에서도 보수적 처리를 우선했다.


### Q3. 왜 피처를 계속 늘리지 않았나요?

- 실제로는 많은 추가가 노이즈로 작용했다.
- 검증 결과, 살아남은 단일 변경만 쌓는 방식이 더 안정적이었다.


### Q4. 실패 실험을 굳이 발표에 넣는 이유가 있나요?

- 실험 과정의 판단 근거를 보여줄 수 있기 때문이다.
- 무엇이 안 먹히는지 정리한 것도 중요한 연구 결과다.


### Q5. 앞으로 가장 유망한 방향은 무엇인가요?

- `v7b`를 유지하면서, 시술 시기 코드 기반의 조합형 피처를 더 정교하게 실험하는 것이다.


## 8. PPT 제작 시 바로 쓰기 좋은 시각자료 목록

- 버전별 CV line chart
- 타깃 비율 bar chart
- feature importance top 15
- 시술 시기 코드별 성공률 bar chart
- 실패 실험 비교 표
- 최종 실험 운영 흐름도


## 9. 발표 자료에 연결하면 좋은 파일

- `README.md`
- `멘토링_정리_20260318.md`
- `train_v8plus_v7b.py`
- `train_v8plus_v7b_lgb_screen.py`
- `submissions/feature_importance_v8plus_v7b_cpu.csv`
- `submissions/cycle_code_profile_summary.txt`


## 10. 최종 추천 구성

가장 추천하는 실제 발표 구성:

- 본문 15장
- 부록 2장
- 발표 20분
- 질의응답 3~5분

이 구성이면 “문제 이해 - 실험 흐름 - 성능 개선 - 실패 분석 - 현재 기준선 - 향후 계획”이 자연스럽게 이어진다.


## 11. PPT 만드는 사람에게 주는 실무 팁

- 슬라이드 1장당 메시지는 1개만 둔다.
- 표는 많아도 5행 정도로 잘라서 핵심만 보여준다.
- 코드 자체보다, 왜 그 코드를 만들었는지와 결과를 우선 설명한다.
- 발표 중 숫자는 3개만 반복한다.
  - `0.74083`
  - `0.74216`
  - `v7b`
- 마지막 장에서는 반드시 “현재 기준선 / 배운 점 / 다음 계획” 3개를 다시 묶어준다.
