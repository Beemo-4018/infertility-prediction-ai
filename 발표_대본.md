# 발표 대본 — v8plus_v7b_final (NotebookLM PPT 기준)
## Team 임신 확률 연구소 | LB 0.74216

> 📌 안내
> - [ ] 는 슬라이드 전환 시점
> - **굵은 글씨** 는 강조 포인트
> - 예상 발표 시간: 약 20분

---

## [ 슬라이드 1 — The Winning Blueprint: LB 0.74216 ]

안녕하세요, Team 임신 확률 연구소 입니다.

저희 발표 제목은 **"The Winning Blueprint"** 입니다.

오늘은 LB 0.74216을 달성한 모델,
`v8plus_v7b_final`을 완전히 분해해서 보여드리겠습니다.

최고 점수 자체보다, **그 점수에 도달하기까지의 실험 흐름과 판단 기준**을 중심으로 말씀드리겠습니다.

---

## [ 슬라이드 2 — 최고 성능을 완성한 4가지 핵심 방정식 ]

결론부터 말씀드리면, 저희 모델의 핵심은 이 4가지입니다.

**130개 피처 + 3-Model Ensemble + Rank Normalization + Optuna Tuning = LB 0.74216**

환경 스펙은 Seed 42, Stratified 5-Fold CV, CPU Only입니다.

*각각이 왜 중요한지, 지금부터 하나씩 설명드리겠습니다.*

---

## [ 슬라이드 3 — 아키텍처 오버뷰: 데이터에서 예측까지 ]

전체 파이프라인은 4단계입니다.

**Step 1 — Data & Feature**
130개 정밀 피처 엔지니어링입니다.
단순한 수치화가 아니라, 도메인 지식과 교호작용을 결합했습니다.

**Step 2 — Auto Optimization**
Optuna 50 Trials 기반 하이퍼파라미터 탐색입니다.
LightGBM의 파라미터를 자동으로 최적화했습니다.

**Step 3 — Engine Training**
LightGBM, XGBoost, CatBoost 세 모델을 K-Fold 교차 검증으로 학습합니다.

**Step 4 — Ensemble & Blending**
Rank Normalization 기반으로 11종 조합을 자동 탐색해서
**매번 최고 CV 조합을 제출**합니다.

---

## [ 슬라이드 4 — Feature Universe: 다각도 데이터 분해 (130 Features) ]

130개 피처는 크게 4개 그룹으로 나뉩니다.

**환자 및 기증자 정보**
나이, 기증자 나이, 과거 이력 비율입니다.
특히 난자기증자가 30세 이하인 경우를 별도 플래그로 만들었습니다. 젊은 기증자의 난자가 품질이 좋다는 의학적 사실을 반영한 겁니다.

**시술 및 배아 상태**
배아 비율 피처 9개, 해동 관련, 연속 실패 횟수입니다.
배아_이식비율이 **피처 중요도 1위**를 차지했습니다.

**시간 및 원인**
시술 간 시간 간격 5개, 불임 원인 분석, 결측 플래그입니다.
*사실 이 시간 간격 피처가 저희 실험에서 가장 극적인 성과를 냈습니다.*
피처 두 개를 추가했을 때 CV가 한 번에 **+0.00675** 올랐습니다.

**고급 조합 및 인코딩**
클리닉 집계, 11개 Target Encoding, 파생 변수입니다.
시술 시기 코드라는 암호화된 컬럼을 다양한 방식으로 조합해서 활용했습니다.

---

## [ 슬라이드 5 — 데이터 밖의 진실: 도메인 지식의 승리 ]

이제 v7b의 핵심 피처를 말씀드리겠습니다.

```python
df['고령_x_단일배아이식'] = df['고령_여부'] * df['단일 배아 이식 여부']
```

**38세 이상 고령 환자**가 **단일 배아 이식(SET)을 선택**한 경우입니다.

왜 이게 강력한 신호냐면,
고령 환자는 난자와 배아 품질이 떨어지는 경우가 많습니다.
그런데 그런 고령 환자에게 의사가 단일 배아 이식을 선택했다면,
그건 **"이 배아는 충분히 좋다"는 의사의 강력한 확신**을 의미합니다.

EDA에서 확인한 결과, 이 조건에 해당하는 케이스의 성공률이 **43.6%** 였습니다.
전체 평균 25.83% 대비 **+17.8%p** 높은 수치입니다.

이 피처 하나가 v7b를 최종 기준 모델로 만든 주인공입니다.
최종 CV는 **0.74083**을 달성했습니다.

*이게 바로 데이터 분석 기술만이 아니라, 의학 도메인 지식이 피처로 직결될 때 나오는 힘입니다.*

---

## [ 슬라이드 6 — 한계 돌파: 3중 교호작용 Target Encoding ]

v6e에서 추가한 피처는 `시술시기코드_난자정자조합_TE`입니다.

**문제 인식**
클리닉 코드와 난자 출처, 정자 출처 각각을 별개로 인코딩하면
특정 병원이 특정 출처 조합에서 보이는 고유한 성공률 패턴을 놓치게 됩니다.

**해결 방법**
클리닉 × 난자 출처 × 정자 출처, 3차원 조합을 하나의 문자열로 만들고
OOF 방식의 Target Encoding을 적용했습니다.

```python
combo = 시술시기코드 + '_' + 난자출처 + '_' + 정자출처
train['시술시기코드_난자정자조합_te'] = OOF_target_encode(combo)
```

**결과**
특정 클리닉의 특정 시술 방식에 대한 **숨겨진 성공률 패턴을 학습**할 수 있었습니다.

---

## [ 슬라이드 7 — 강력하고 다채로운 3-Model 엔진 ]

세 모델이 각자 다른 역할을 담당합니다.

**LightGBM — Main Engine**
핵심 예측을 담당합니다.
USE_OPTUNA = True 설정으로 50 Trials 자동 탐색해서 최적 하이퍼파라미터를 추출합니다.

**XGBoost — Stability Sub-engine**
기본 파라미터를 사용합니다.
과적합을 방지하고 예측의 안정성을 부여하는 역할입니다.

**CatBoost — Categorical Sub-engine**
범주형 데이터 처리에 특화된 로직으로 보완 예측을 수행합니다.
특히 클래스 불균형 데이터에 강한 특성이 있어서, 25.83% 불균형인 이 데이터에 유리했습니다.

*세 모델이 서로 다른 방식으로 데이터를 바라보기 때문에 앙상블 효과가 발생합니다.*

---

## [ 슬라이드 8 — 자율 주행 앙상블 시스템 ]

저희 앙상블 시스템의 가장 독창적인 부분입니다.

기존 방식은 연구자가 직관이나 수동 실험에 의존해서 모델 가중치를 부여합니다.
저희는 **코드가 자동으로 최적 조합을 찾아냅니다.**

LGB, XGB, CAT 세 모델의 가능한 **11가지 앙상블 조합**을
단순평균, 성능 가중, LGB 강조, Rank 기반 등
코드가 자동으로 순회하며 평가합니다.

그리고 매 런타임마다 최고 CV를 달성하는 최적의 조합을 **스스로 선택**해서 최종 결과로 제출합니다.

**인간의 편향이 개입하지 않는 구조**입니다.

---

## [ 슬라이드 9 — 스코어의 왜곡을 바로잡다: Rank Normalization ]

앙상블의 핵심 기법인 Rank Normalization을 설명드리겠습니다.

**문제**
LightGBM, XGBoost, CatBoost는 각각 예측 확률의 스케일과 분포 형태가 다릅니다.
이 상태에서 단순 평균을 내면 앙상블 성능이 훼손될 수 있습니다.

**해결**
예측값을 절대적인 확률 수치가 아닌 **순위(Rank) 기반으로 변환**한 뒤 정규화해서 블렌딩합니다.

```python
r_lgb = rankdata(oof_lgb) / len(oof_lgb)
r_xgb = rankdata(oof_xgb) / len(oof_xgb)
r_cat = rankdata(oof_cat) / len(oof_cat)
```

**임팩트**
스코어 분포의 근본적 차이를 보정하여 앙상블 효율을 극대화했고,
최종 리더보드 점수에서 **+0.00126 상승에 기여**했습니다.

---

## [ 슬라이드 10 — 통제된 진화: 실험 설계의 철학 ]

버전별 단일 변경 원칙, **Single Change Principle**입니다.

*사실 저희가 처음부터 이렇게 체계적이었던 건 아닙니다.*

초반에는 피처를 무작정 늘렸습니다.
150개 이상까지 추가했는데 오히려 CV가 낮아졌습니다.
노이즈가 신호를 덮고 있었던 겁니다.

그래서 원칙을 바꿨습니다.
**한 버전 업데이트 시, 오직 1개의 피처나 로직만 추가해서
그 효과를 완벽하게 고립시키고 검증한다.**

그 결과가 이 계단 모양의 성장 곡선입니다.

- **v8plus_v3** — 기본 피처 127개 베이스라인, CV 0.74076
- **v8plus_v5** — 파생변수 3개 추가, CV 상승
- **v6e** — 3중 교호작용 TE 단일 추가, CV 추가 상승
- **v7b (최종)** — 고령 × 단일배아이식 교호작용 단일 추가, **CV 0.74083 최고점**

작은 변경들이 쌓여서 최고 점수를 만들었습니다.

---

## [ 슬라이드 11 — 다음 스텝을 위한 레거시: OOF 저장 시스템 ]

마지막으로 코드의 확장성 설계를 말씀드리겠습니다.

저희 코드는 K-Fold 훈련 과정에서 생성된 각 모델의 OOF 예측값을
`oof_lgb.npy`, `oof_xgb.npy`, `oof_cat.npy`로 **자동 저장**합니다.

이게 단순히 결과를 저장하는 것처럼 보이지만,
저장된 OOF 파일들은 향후 **Meta Model 구축 및 Stacking 앙상블의 핵심 재료**로 즉시 재활용 가능합니다.

단일 스크립트의 종료로 끝나지 않고, 다음 실험으로 이어지는 **레거시를 남기는 구조**입니다.

---

## [ 슬라이드 12 — Why This is the Winning Code ]

마지막 슬라이드입니다.

이 코드가 최고 성능을 낸 이유를 세 가지로 요약할 수 있습니다.

**Domain Intelligence**
단순 통계를 넘어선 의학적 인사이트.
고령 × 단일배아이식 교호작용이 대표적입니다.

**Robust Engineering**
130개 피처와 3차원 TE를 안정적으로 다루는 견고한 파이프라인.

**Creative Optimization**
Rank Normalization 기반 11종 자동 앙상블 탐색 시스템.

이 세 가지가 교차하는 지점에서 **LB 0.74216**이 나왔습니다.

---

마무리 멘트:

"데이터 분석의 정교함, 도메인 지식의 깊이, 엔지니어링의 창의성이
완벽하게 결합된 최종 모델을 만들기 위해 노력했습니다.

이상으로 Team Beemo의 발표를 마치겠습니다. 감사합니다."

---

## 예상 Q&A

**Q1. 왜 CatBoost가 아닌 LightGBM이 Main Engine인가요?**

LightGBM이 이 데이터에서 Optuna 튜닝 후 가장 높은 단일 CV를 기록했기 때문입니다.
다만 CatBoost가 범주형 처리에서 보완적 역할을 하기 때문에 앙상블에서 중요한 비중을 차지합니다.

**Q2. 피처를 150개까지 늘렸다가 왜 오히려 낮아졌나요?**

노이즈가 신호를 덮었기 때문입니다.
의미 없는 피처가 많아질수록 모델이 중요한 신호에 집중하기 어렵습니다.
그래서 Single Change Principle로 전략을 바꾸고, 검증된 피처만 쌓는 방식을 택했습니다.

**Q3. Rank Normalization이 왜 단순 평균보다 효과적인가요?**

각 모델의 예측 확률 분포가 다르기 때문입니다.
LightGBM은 넓은 분포, CatBoost는 좁은 분포를 가질 수 있습니다.
단순 평균하면 분포가 넓은 모델에 가중치가 쏠립니다.
Rank로 변환하면 분포 차이를 제거하고 상대적 순서만 반영할 수 있습니다.

**Q4. OOF를 저장하는 이유가 무엇인가요?**

나중에 스태킹 앙상블을 할 때 재사용할 수 있기 때문입니다.
매번 재학습하지 않고 저장된 OOF를 메타 모델의 입력으로 바로 사용할 수 있어서
실험 속도가 크게 향상됩니다.

**Q5. 시술 시기 코드가 암호화되어 있는데 어떻게 활용했나요?**

코드 자체를 해독하려 하지 않았습니다.
Target Encoding으로 수치화해서 모델이 클리닉 정보를 간접적으로 학습하게 했습니다.
특히 클리닉 × 난자출처 × 정자출처 3중 조합까지 활용해서
특정 병원의 특정 시술 방식에 숨겨진 성공률 패턴을 포착했습니다.
