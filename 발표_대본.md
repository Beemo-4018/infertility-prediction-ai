# 난임 환자 임신 성공 여부 예측 AI 해커톤
## 발표 대본 (20분)

> 📌 발표자 안내
> - [ ] 표시는 슬라이드 전환 시점
> - **굵은 글씨**는 강조 포인트
> - *기울임*은 자연스럽게 넘어가는 연결 멘트

---

## [ 슬라이드 1 — 표지 ]

안녕하세요, Team 임신 확률 연구소입니다.

저희 팀은 이번 해커톤에서 난임 환자의 임신 성공 여부를 예측하는 AI 모델을 개발했습니다.

오늘 발표에서는 최고 점수 자체보다,
**그 점수에 도달하기까지의 실험 과정과 판단 기준**을 중심으로 말씀드리겠습니다.

결론부터 말씀드리면, 저희 팀의 최고 LB 점수는 **0.74218**입니다.

---

## [ 슬라이드 2 — 문제 정의 ]

이번 대회의 과제는 난임 환자의 시술 데이터를 바탕으로
**임신 성공 여부를 확률로 예측**하는 것입니다.

입력 데이터는 환자 정보, 시술 이력, 배아와 난자, 정자 관련 정보가 69개 컬럼으로 구성되어 있고,
출력은 임신 성공 확률 0에서 1 사이의 값입니다.
평가 지표는 ROC-AUC입니다.

*여기서 한 가지 중요한 점이 있습니다.*

이 데이터는 단순한 분류 문제가 아니었습니다.
의료 데이터이기 때문에 **결측치의 의미, 범주의 맥락, 시술 유형에 따라 변수의 해석이 달라졌습니다.**
그래서 모델링 기술만큼, 데이터를 제대로 해석하는 것이 중요했습니다.

---

## [ 슬라이드 3 — 데이터 개요 ]

데이터 규모는 train이 256,351행, test가 90,067행입니다.

한 가지 주목할 점은 **임신 성공률이 25.83%** 라는 것입니다.
약 2.87대 1의 클래스 불균형이 있었습니다.

샘플 수는 충분하지만,
결측치가 많은 컬럼, 희소한 범주형 변수, 시술 맥락에 따라 의미가 달라지는 변수들이 많아서
단순한 모델링만으로는 한계가 있었습니다.

---

## [ 슬라이드 4 — 검증 전략 ]

모든 실험은 **동일한 기준에서 비교해야 의미가 있습니다.**

저희는 Stratified 5-Fold OOF 방식으로 검증했고,
빠른 실험이 필요할 때는 LightGBM 단독 3-Fold 스크리닝을,
최종 비교 시에는 LightGBM, XGBoost, CatBoost 앙상블 5-Fold를 사용했습니다.

그리고 Data Leakage 방지 원칙을 엄격하게 지켰습니다.
Target Encoding은 반드시 K-Fold OOF 방식으로,
결측치는 train 기준 median만 사용하고,
LabelEncoder는 train만 fit해서 test에 적용했습니다.

특히 Pseudo-Labeling은 대회 규정상 실격 처리되는 방식이기 때문에 절대 사용하지 않았습니다.

---

## [ 슬라이드 5 — EDA 도메인 인사이트 ]

본격적인 모델링 전에 데이터를 깊이 분석했는데,
**의학적으로 의미 있는 강력한 신호들**을 발견했습니다.

가장 인상적인 발견은 이 두 가지입니다.

첫째, **SET와 BLASTOCYST가 함께 있는 경우, 성공률이 43.6%** 였습니다.
전체 평균이 25.83%이니까 무려 17.8%p 높은 수치입니다.
단일 배아 이식에 배반포까지 조건이 맞으면 성공률이 크게 올라간다는 의학적 사실이 데이터에 그대로 담겨 있었습니다.

둘째, **배아 목적이 저장이나 기증인 경우, 성공률이 0~0.1%** 였습니다.
임신을 목적으로 하지 않는 시술이니 당연한 결과인데,
이 신호를 피처로 잘 잡아내는 것이 중요했습니다.

이 인사이트들이 이후 피처 설계의 출발점이 됐습니다.

---

## [ 슬라이드 6 — 초기 시도와 방향 전환 ]

*사실 저희가 처음부터 이렇게 체계적으로 접근했던 건 아닙니다.*

초반에는 "피처가 많을수록 좋다"는 생각으로 접근했습니다.
나이, 배아 비율, 불임 원인, 시술 유형, 교호작용 피처까지
**피처를 150개 이상까지 늘렸습니다.**

그런데 결과가 오히려 나빠졌습니다.
CV가 떨어지기 시작했습니다.

*왜 그럴까 고민했는데, 답은 간단했습니다.*
**노이즈가 신호를 덮고 있었던 겁니다.**

그래서 방향을 완전히 바꿨습니다.

**Clean Baseline 전략**으로 돌아갔습니다.
의학적으로 의미 있는 피처만 남기고, 피처 수를 15개까지 줄였습니다.
그리고 거기서 피처 중요도를 정확히 파악한 뒤, 하나씩 추가 검증하는 방식으로 바꿨습니다.

결과적으로 이 전환이 성능 향상의 핵심이 됐습니다.

---

## [ 슬라이드 7 — 시간 간격 피처 발견 ]

Clean Baseline 실험에서 가장 큰 발견을 했습니다.

```python
df['혼합_이식_간격'] = df['배아 이식 경과일'] - df['난자 혼합 경과일']
df['해동_이식_간격'] = df['배아 이식 경과일'] - df['배아 해동 경과일']
```

이 두 피처를 추가했을 때 CV가 **0.72509에서 0.73184로, +0.00675 점프**했습니다.
지금까지 실험 중 단일 피처 추가로 가장 큰 향상이었습니다.

이유가 뭔가 생각해보면,
혼합에서 이식까지의 간격이 5일 이상이면 배반포까지 배양된 좋은 배아라는 신호입니다.
배양 기간이 길수록 배아가 건강하다는 의학적 사실이 그대로 반영된 거였습니다.

*이게 바로 데이터 해석이 모델보다 중요한 이유입니다.*

---

## [ 슬라이드 8 — feature_search 자동화 ]

방향을 바꾼 뒤 또 하나의 문제가 있었습니다.
후보 피처를 하나씩 수동으로 추가하고 CV를 확인하는 게 너무 느렸습니다.

그래서 **feature_search 자동화 스크립트**를 만들었습니다.

```python
for feat in CANDIDATE_FEATURES:
    cv    = measure_cv(BASE_FEATURES + [feat], y)
    delta = cv - base_cv
```

이 스크립트 하나로 후보 피처 40개 이상을 한 번에 측정할 수 있었습니다.

결과를 보면 흥미로운 점이 있습니다.

배아_저장비율이 +0.00558로 압도적 1위였고,
난자기증자_나이_수치가 +0.00068로 의미 있는 기여를 했습니다.

반면 직관적으로 좋아 보였던
클리닉_나이별성공률은 오히려 -0.00018로 마이너스였습니다.
이미 다른 피처와 중복된 정보였기 때문입니다.

**직관을 믿지 말고, 검증을 믿어야 한다**는 걸 다시 한번 확인했습니다.

---

## [ 슬라이드 9 — 시술 시기 코드의 힘 ]

데이터에 `TRCMWS`, `TRDQAZ` 같은 암호화된 코드가 있었습니다.
처음엔 그냥 ID인 줄 알았습니다.

그런데 이 코드를 Target Encoding으로 수치화하자마자,
피처 중요도에서 최상단으로 올라왔습니다.

**시술 시기 코드는 단순 ID가 아니라,
어느 병원에서 어느 시기에 시술받았는지를 대변하는 핵심 정보**였습니다.

어떤 병원은 고령 환자의 동결 배아 이식 기술이 뛰어나고,
어떤 병원은 PCOS 환자 배양을 잘합니다.
이 차이가 코드 하나에 압축되어 있었던 겁니다.

저희는 이 코드를 다양하게 조합해서 활용했습니다.

- 클리닉별 성공률 (단순 TE)
- 클리닉 × 나이 조합 TE
- 클리닉 × 시술유형 조합 TE
- 클리닉 × 난자출처 × 정자출처 3중 조합 TE

---

## [ 슬라이드 10 — 버전별 성능 변화 ]

실험 결과를 버전별로 정리하면 이렇습니다.

v3에서 Target Encoding과 도메인 피처로 LB 0.74198을 달성했고,
v5에서 클리닉 집계 피처를 추가했습니다.
v8에서 미사용 컬럼을 발굴하고 LGB Optuna 튜닝을 적용했습니다.

그리고 오늘 발표의 기준 코드인 **v8plus_v7b에서 최고 LB 0.74216**을 달성했습니다.

성능 향상의 패턴을 보면 한 가지 사실이 명확합니다.
**큰 구조 변경보다, 해석 가능한 단일 피처 하나씩 추가가 더 효과적이었습니다.**

---

## [ 슬라이드 11 — v7b 핵심 피처 ]

v7b에서 추가한 피처는 단 하나입니다.

```python
df['고령_x_단일배아이식'] = df['고령_여부'] * df['단일 배아 이식 여부']
```

38세 이상 고령 환자가 단일 배아 이식을 선택한 경우입니다.

왜 이게 강력한 신호냐면,
고령 환자에게 단일 배아 이식을 선택했다는 것 자체가 **의사가 이 배아가 충분히 좋다고 판단했다는 신호**이기 때문입니다.
EDA에서 확인한 43.6% 성공률 패턴이 그대로 반영된 피처입니다.

이렇게 **도메인 지식이 피처로 직결될 때 가장 큰 효과**가 났습니다.

---

## [ 슬라이드 12 — 앙상블 전략 ]

최종 앙상블 방식도 차별화 포인트입니다.

저희는 매 실험마다 **11가지 앙상블 조합을 자동으로 비교**해서 최고 CV 조합을 제출했습니다.

단순 평균, 성능 가중 평균, LGB 강조, LGB+CAT 조합 등
다양한 방식을 코드가 자동으로 계산하고 순위를 매겨줍니다.

그 중에서 특히 효과적이었던 게 **Rank Normalization**입니다.

```python
r_lgb = rankdata(oof_lgb) / len(oof_lgb)
r_xgb = rankdata(oof_xgb) / len(oof_xgb)
r_cat = rankdata(oof_cat) / len(oof_cat)
```

확률값 대신 순위값으로 정규화해서 블렌딩하는 방식입니다.
각 모델의 score 분포 차이를 제거하고 상대적 순서만 반영하기 때문에
LB에서 +0.00126의 효과를 확인했습니다.

*그리고 한 가지 중요한 교훈이 있었습니다.*

블렌딩하면 무조건 좋아진다고 생각했는데 아니었습니다.
제출 파일들의 상관관계를 분석해보니 v5, v8, v8_tuned 모두 0.998 이상이었습니다.
**같은 피처셋 기반 모델들은 블렌딩해도 평균을 내는 것에 불과했습니다.**
진짜 다양성은 완전히 다른 피처셋에서 나온다는 걸 배웠습니다.

---

## [ 슬라이드 13 — 실패 교훈 ]

실패한 실험들도 솔직하게 공유드리겠습니다.

**풀 스태킹**은 CV가 0.74에서 0.73481로 폭락했습니다.
3개 모델의 OOF와 원본 피처 135개를 메타 모델에 전부 넣었더니 오버피팅이 발생했습니다.
메타 피처는 OOF 예측값만 사용해야 한다는 걸 배웠습니다.

**TabNet + GPU** 실험은 25만행 규모에서 트리 모델보다 불안정했습니다.
GPU는 수백만 행 이상일 때 진가를 발휘하고, 이 정도 크기에서는 CPU가 더 나았습니다.

그리고 앞서 말씀드린 것처럼 **피처 183개로 확장**했을 때 오히려 CV가 낮아졌습니다.
피처 수보다 피처 품질이 중요하다는 가장 중요한 교훈이었습니다.

---

## [ 슬라이드 14 — 최종 결과 ]

최종 성과를 정리하면 이렇습니다.

최고 CV AUC **0.74083**, 최고 LB AUC **0.74216**입니다.
1위와의 차이는 0.00019였습니다.

제출 이력을 보면 v3에서 처음 0.74198을 달성하고,
블렌딩으로 0.74205, 그리고 v8plus_v7b에서 0.74216으로 마무리했습니다.

---

## [ 슬라이드 15 — 결론 ]

마지막으로 이번 실험에서 얻은 핵심 3가지를 말씀드리겠습니다.

**첫째, 데이터 해석이 모델보다 중요했습니다.**

EDA에서 발견한 의학 도메인 인사이트가 가장 큰 성능 향상을 만들었습니다.
Day5 배반포, 저장/기증 목적 구분, 시술 시기 코드 조합이 핵심이었습니다.

**둘째, 검증 가능한 단일 변경이 대규모 변경보다 잘 먹혔습니다.**

150개까지 늘렸다가 오히려 성능이 떨어진 경험이 이 교훈을 만들었습니다.
기준선을 고정하고 하나씩 추가하는 방식이 가장 안정적이었습니다.

**셋째, 블렌딩은 다양성이 전제되어야 효과가 있습니다.**

상관관계 0.998 이상인 모델끼리 블렌딩은 의미가 없었습니다.
진짜 다양성은 다른 피처셋 또는 다른 알고리즘에서 나옵니다.

---

마무리 멘트:

"복잡한 기법보다, 데이터의 의미를 해석하고 검증 가능한 단일 변경을 쌓는 것.
이것이 이번 실험에서 얻은 가장 중요한 인사이트입니다."

이상으로 발표를 마치겠습니다. 감사합니다.

---

## 예상 Q&A

**Q1. 왜 CatBoost 단독이 아니라 앙상블을 선택했나요?**

각 모델이 포착하는 패턴이 조금씩 다르기 때문입니다.
실제로 CatBoost는 클래스 불균형 데이터에 강한 특성이 있어서 단독으로도 성능이 좋았지만,
LGB와 XGB와 함께 앙상블했을 때 CV가 더 높게 나왔습니다.
다만 최근에는 모델 다양성보다 피처 개선의 기여가 더 컸습니다.

**Q2. 왜 복잡한 결측치 처리를 하지 않았나요?**

결측치 자체가 중요한 신호일 수 있어서 단순 대체가 항상 유리하지 않았습니다.
실제로 결측 플래그를 피처로 만들어서 사용했고,
재현성과 Leakage 방지 측면에서도 train median 기준의 보수적 처리를 우선했습니다.

**Q3. 왜 피처를 계속 늘리지 않았나요?**

실제로 초반에 150개 이상까지 늘렸다가 CV가 오히려 낮아지는 경험을 했습니다.
노이즈가 증가해서 신호를 덮기 때문입니다.
그 이후로 기준선을 고정하고 살아남은 단일 변경만 쌓는 방식으로 전략을 바꿨습니다.

**Q4. 시술 시기 코드가 암호화되어 있는데 어떻게 활용했나요?**

코드 자체를 해독하려 하지 않고, Target Encoding으로 수치화했습니다.
클리닉별 성공률, 클리닉×나이 조합, 클리닉×시술유형 조합 등
다양한 형태로 인코딩해서 모델이 클리닉 정보를 간접적으로 학습하게 했습니다.

**Q5. 앞으로 더 개선할 수 있는 방향이 있다면?**

크게 두 가지입니다.
첫째는 SMOTE를 활용한 클래스 불균형 처리입니다. 아직 시도하지 못했습니다.
둘째는 완전히 다른 피처셋 기반의 모델을 만들어서 블렌딩 다양성을 확보하는 것입니다.
상관관계 분석 결과 현재 모델들이 0.998 이상으로 너무 비슷해서
진짜 다양성을 가진 모델이 추가되면 블렌딩 효과를 볼 수 있을 것입니다.
